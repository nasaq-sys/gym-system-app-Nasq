# Ultra Gym — AI Rules & Guidelines (قواعد وتعليمات الذكاء الاصطناعي)

هذه القواعد ملزمة للذكاء الاصطناعي في هذا المشروع، ويجب الالتزام بها بدقة في كل محادثة وعملية برمجية دون التصرف الفردي أو كسر أي ميزة سابقة:

---

## 1. بيانات الاعتماد وكلمات المرور (Authentication & Passwords)
- **كلمات المرور في Airtable:** تُحفظ وتُقارن كنص عادي (Plain Text) دائماً وفق تعليمات العميل. **ممنوع تشفيرها أو تحويلها إلى Hash (Bcrypt)**.

## 2. حماية الأمان والبيانات السرية (Security & Secrets)
- يُمنع منعاً باتاً كتابة أو تسريب مفاتيح الـ API أو المتغيرات السرية (`AIRTABLE_TOKEN`, `SESSION_SECRET`, `UPSTASH_REDIS_REST_TOKEN`...) داخل مكونات العميل (`"use client"`) أو طباعتها في الـ `console.log`.
- أي اتصال بقاعدة البيانات يجب أن يتم حصراً عبر خوادم الـ API الخلفية (`/api/...`).

## 3. عدم الحذف العشوائي (No Unapproved Deletions)
- **ممنوع حذف أي ميزة أو ملف أو كود شغال** بدون طلب وتأكيد صريح من المستخدم.
- الحفاظ على مكونات **ShadCN UI** ومكونات واجهة المستخدم الأساسية.

## 4. الودجت (Widgets)
- تم إلغاء جميع ودجات الشاشة الرئيسية (Home Widgets) في الويب وتطبيقات Android و iOS بناءً على طلب العميل؛ **يُمنع إعادة إنشائها أو إضافتها**.

## 5. الفصل التام للصلاحيات (Strict Role-Based Access)
- الحفاظ على العزل التام بين صفحات المتدرب (`/home`, `/workouts`, `/profile`)، وصفحات المدربين (`/trainer`)، وصفحات الإدارة (`/admin`).
- حماية أي مسار أو endpoint جديد والتحقق من صلاحية المستخدم عبر `src/middleware.ts` و `serverAuth.ts`.

## 6. مسارات الإشعارات (Notification Routing)
- الإشعارات العامة والافتراضية يجب أن توجه المتدرب دائماً إلى الصفحة الرئيسية `/home` أو الصفحات المخصصة مثل `/workouts`.
- **ممنوع توجيه المستخدم العادي إلى صفحات الإدارة أو مسارات غير مخصصة له**.

## 7. معايير التصميم والألوان (UI & Design System)
- الالتزام بنظام الألوان المعرّف في `src/app/globals.css` باستخدام كلاسات Tailwind الجاهزة:
  - `bg-background`, `text-foreground`, `bg-card`, `bg-card-hover`, `border-border`, `bg-primary`.
- **ممنوع كتابة ألوان Hex عشوائية** داخل المكونات.
- ضمان التوافق التام مع الوضعين الداكن (Dark Mode) والفاتح (Light Mode).
- القوائم والنوافذ المنبثقة (Popovers) يجب أن تستخدم نظام **Portal** لعدم اقتصاصها بواسطة البطاقات المحيطة (`overflow-hidden`).

## 8. منع الإيموجي واستخدام الأيقونات حصراً (No Emojis — Icons Only)
- **ممنوع منعاً باتاً استخدام الرموز التعبيرية (Emojis)** في أي مكان داخل واجهة المستخدم، العناوين، الأزرار، أو البطاقات.
- الاعتماد الحصري على أيقونات **Lucide React** (`lucide-react`) وتنسيقها بأحجام وألوان متناسقة مع نظام التصميم لإعطاء مظهر احترافي وعصري.

## 9. مكونات الواجهة (ShadCN UI Components)
- الاعتماد الإجباري على مكونات **ShadCN UI** الجاهزة والمعرّفة في المشروع تحت `src/components/ui/` (مثل `Button`, `Card`, `Badge`, `Dialog`, `Popover`, `Sheet`, `Select`, `Input`, `Tabs`...) وتجنب بناء مكونات بدائية من الصفر.

## 10. التجاوب مع الموبايل والنوافذ المنزلقة (Mobile-First & Sheets)
- ضمان التجاوب بنسبة 100% مع كافة مقاسات شاشات الهواتف دون حدوث أي شريط تمرير أفقي (`overflow-x`).
- أهداف اللمس (Touch Targets) للأزرار يجب ألا تقل عن `44px`.
- في شاشات الموبايل، يُفضل استخدام النوافذ السفلية المنزلقة (`Sheet` من ShadCN) للعمليات التفاعلية السريعة (إضافة وجبة، اختيار تمرين، تعديل بيانات) لسهولة الاستخدام بالإبهام.

## 11. السلاسة والتمرير (Smooth Scrolling)
- الحفاظ على السلاسة الأصلية للتمرير (Native Momentum Scrolling) في الموبايل والويب.
- **ممنوع وضع `overscroll-behavior: none` أو قفل الـ touch** على `body` أو `html` أو `main`.

## 12. حالات التحميل والفراغ والأخطاء (Skeletons & Empty States)
- أي مكوّن أو بطاقة تقوم بجلب بيانات يجب أن تتضمن:
  1. هيكل تحميل بصري (`Skeleton`) أثناء انتظار الاستجابة.
  2. واجهة واضحة للحالة الفارغة (`Empty State`) مع أيقونة مناسبة وزر اتخاذ إجراء.
  3. رسالة خطأ واضحة مع زر لإعادة المحاولة عند حدوث خلل في الشبكة.

## 13. التنسيق الموحد للبيانات (Centralized Formatting Helpers)
- استخدام دوال التنسيق المركزية المعرفة في `src/lib/format.ts` (مثل `formatDate`, `formatWeight`, `formatDuration`, `fmtCurrency`) وتجنب كتابة دوال تنسيق يدوي مكررة في كل صفحة.

## 14. الترجمة واللغات (i18n Localization)
- أي نص جديد يُضاف للواجهة يجب توثيقه في ملفي الترجمة:
  - `src/i18n/ar.json` (العربية)
  - `src/i18n/en.json` (الإنجليزية)
- مع إضافة نصوص بديلة آمنة باللغتين لتجنب ظهور مفاتيح الكود للمستخدم.

## 15. الأداء وتخفيف استهلاك API (Performance & Caching)
- الحفاظ على نظام الكاش للعميل (`clientCache.ts`) لتفادي الضغط على خوادم Airtable وسيرفر التطبيق.
- تجنب إرسال طلبات متكررة غير ضرورية وتطبيق Rate Limiting على المسارات الحساسة.

## 16. الفحص الإجباري للبناء (Build Health)
- بعد أي تعديل، يجب التأكد من خلو الكود من أخطاء TypeScript وأن عملية البناء `npm run build` تنتهي بنجاح تام (Exit Code 0).
